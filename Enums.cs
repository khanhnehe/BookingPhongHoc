using System.ComponentModel;

namespace BookingPhongHoc
{
    public class Enums
    {
        public enum Role
        {
            gvtg = 1,

            gvt = 2,

            admin = 3
        }

        public enum StatusBooking
        {
            pending = 1,

            approved = 2,

            cancel = 3
        }
    }
}
